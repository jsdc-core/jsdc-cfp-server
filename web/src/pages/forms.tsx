import { PagePlaceholder } from "@/components/page-placeholder";

export default function FormsPage() {
  return (
    <PagePlaceholder
      title="表單管理"
      description="建立、編輯與發布表單（Call for Papers / 報名表等）。"
    />
  );
}
